/**
 * Observer pattern là một mẫu thiết kế phần mềm mà một đối tượng, gọi là subject,
 * duy trì một danh sách các thành phần phụ thuộc nó, gọi là observer
 * Và thông báo tới chúng một cách tự động về bất cứ thay đổi nào, thường thì bằng cách
 * gọi 1 hàm của chúng.
 * VD: SonTungMTP đăng tải bất kỳ sự kiện gì,ảnh,..thì nhiều người hâm mộ sẽ biết được điều đó
 * và tương tác
 */